import React from 'react';

const EmployeeDashboard = () => {
  return (
    <div className="dashboard">
      <h2>Employee Dashboard</h2>
      {/* Add components for displaying assigned tasks, time reporting, etc. */}
    </div>
  );
};

export default EmployeeDashboard;