import React from 'react';

const AdminDashboard = () => {
  return (
    <div className="dashboard">
      <h2>Admin Dashboard</h2>
      {/* Add components for displaying user count, ongoing orders, messages, etc. */}
    </div>
  );
};

export default AdminDashboard;