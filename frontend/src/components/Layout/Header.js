import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1>07App</h1>
      {/* Add navigation links if needed */}
    </header>
  );
};

export default Header;